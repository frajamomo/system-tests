package tests

// Shared string literals used across the far-operator test files.
const (
	fenceAgentsRemediationKind = "FenceAgentsRemediation"

	flagAccessKey     = "--access-key"
	flagSecretKey     = "--secret-key"
	flagRegion        = "--region"
	flagAction        = "--action"
	flagSkipRaceCheck = "--skip-race-check"

	keySpec  = "spec"
	globYAML = "*.yaml"

	actionReboot = "reboot"
)
